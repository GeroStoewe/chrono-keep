import React from "react";

/**
 * DividerLP component
 *
 * A simple horizontal divider with a top border and margin at the bottom, typically used to separate sections.
 *
 * @returns {JSX.Element} The rendered divider component.
 */
const DividerLP: React.FC = () => {
  return <div className="w-full border-t-2 mb-20 border-gray-300"></div>;
};

export default DividerLP;
